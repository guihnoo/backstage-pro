---
name: fullstack-feature
description: Feature REST ponta-a-ponta — contrato, Fastify, cliente, UI
---

# Full-stack

1. Contrato request/response.
2. Rotas em `server/` com validação.
3. Cliente em `src/lib/` ou `src/api/` usando `apiUrl()`.
4. UI + tratamento de erro.
5. OWASP: skill `security-api-review`.
