# Copie para `.env` na raiz (não commitar segredos).

# Frontend — em dev com proxy Vite, usar `/api` (mesmo origin).
VITE_API_URL=/api

# API (server/index.js)
PORT=3001
HOST=0.0.0.0

# Origem do Vite dev (ou lista separada por vírgula em cenários avançados)
FRONTEND_ORIGIN=http://localhost:5173
