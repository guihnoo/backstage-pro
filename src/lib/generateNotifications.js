// Notificações agora são geradas em memória pelo NotificationCenter.
// Esta função mantida apenas para não quebrar o import em AppLayout.
export async function generateUserNotifications(_userId) {}
